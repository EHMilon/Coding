1.Start
2.make a function isPrime(int n) 
    if n<=1 then it is not a prime number 
        return false/0;
    if n>1 then, usage for loop 
        for(int i=2, i<=Math.sqrt(n), i++)
            if(n%i==0) 
                return false;
    or, return true;
3.to find a number prime or not
    take input from user and initialize a variable num
    if(isPrime(num))
        print this is a prime number
    else
        print this is not a prime number
3.to print prime number between 1 to 100
    usage for loop
    for(int i=2, i<=100, i++)
        if(isPrime(i))
            print i

4.exits